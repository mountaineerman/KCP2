SOFTWARE ==================================================================================================================================

Solve stepper motor performance
	Investigate burst mode and strange buffer bug
		Add checksums to packets?

	Get it working "as-is" to assess usability:
		Try to move the motors up to X number of steps during the KMegaService::standardOperatingMode() loop
		Selectively send Stepper info from KKIM to KMega (search "//TODO: EnableSteppers")
			Modules --- EXAMPLE: Module I: Fuel
			OP.java (stepper setup & calibration)
			PacketAssembler.assembleOutputRefreshPacket (send data)
			ControlPanel.refresh (calculate stepper gauge desired positions) --- EXAMPLE: Module I: Fuel
			
		Play with inputRefreshPacket send frequency
		Play with outputRefreshPacket send frequency
		Play with number of active gauges >> KKIM: ControlPanel.refresh(). Search for "setDesiredPosition"
		Play with max number of steps (per gauge priority) per KMega::standardOperatingMode() cycle
		
		
	Clean up StepperMotor class...
	Re-enable commented sections (search keyword "TODO: Stepper Logic disabled until performance is fixed (do not modify)"
	Add friction to the needle holes so they don't slip?+
	Adjust needle positions:
		Mach
		Speed
		Vertical Speed
	Re-calibrate motors (KKIM: OP.java)
	CONSIDER: Add interrupt logic for stepper motors (don't let the packet processing slow down the motor movement)

Extract all needed information from kRPC. Measure performance.
	kRPC: streams?
	Replace LED brightness (0-4095) with 1 byte (0-255).
Determine necessary loop interval time to keep things "snappy"

Change Camera Mode when switching between RKT/PLN/RVR

Phone display...

Implement TRIM

Fix action groups: duplicate signals sometimes sent? PV and LADR seem ok, but AG1+etc "flaky".

KMega: Integrate MUTE switch. If ON:
	a) All possible outputs are disabled
	b) OutputRefreshPackets are ignored

Maybe:
Integrate ATNV (autonavigation) momentary switch
Rename master > main
Find a way to stop the kmega COM port from changing periodically
Consider using PlatformIO (+VS Code?) instead of Arduino IDE

See KCP2_Lessons_Learned.txt

HARDWARE ==================================================================================================================================

Stop SP3T switch guards from turning
Physically re-do label for air density gauge (red = 100, green = 0)
Replace LEDs that are too bright or dim them with bigger resistors
Touch up weak paint areas near backlit icons
Paint panel behind modules to keep the gaps from looking silvery