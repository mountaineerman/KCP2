HARDWARE ==================================================================================================================================

Add friction to the needle holes so they don't slip?+
Adjust needle positions (+re-calibrate motors (KKIM: OP.java)):
	Mach
	Speed
Stop SP3T switch guards from turning
Physically re-do label for air density gauge (red = 100, green = 0)
Replace LEDs that are too bright or dim them with bigger resistors
Touch up weak paint areas near backlit icons
Paint panel behind modules to keep the gaps from looking silvery
Fix Reset circuit (see README: Fix 2b), update schematic.

SOFTWARE ==================================================================================================================================

Solve stepper motor performance
	Prototype timing based on additional microprocessors responsible for stepping motors
		KMega new responsibilities
			KMegaService::startupMode()
				refactor blockRunAllGearedSteppersToPosition (guess travel time) - CURRENTLY DISABLED
				refactor sweepGearedStepperMotorsThroughMaxMin - CURRENTLY DISABLED
			KMegaService::standardOperatingMode()
				packetAssembler.assembleNGHACalibrationRequest()
				packetAssembler.assembleNGHBCalibrationRequest()
	Refactor StepperMotor2 (+...) given that KMega no longer directly controls the gauges
		runStepperIfNecessary
		ControlPanel: diagnostic modes...
	
	Test Complete System:
		Symptoms:
			NGH A/B sweep gauges, indicating they do at least startup.
			KMega LEDs update as per KKIM, indicating it's receiving outputRefreshPackets from KKIM
			Altitude gauge shows ~70m, indicating it's receiving altitudePackets from KMega
				KMega is "hitting" the code that should be assembling and sending the gauge packets.
		
			NGH B is NOT getting gauge packets on the serial port...
			NGH B is getting bytes on the serial port.
			
			When NGH B starts (KKIM off, KMega on), it sees 24 bytes in the serial buffer.
			When NGH B starts (KKIM on, KMega on), it sees lots of bytes in the serial buffer.
			KKIM on, KMega on:
				NGH B is seeing 3 delimiter bytes in a row.
				NGH B is seeing a packetBufferCursor at full size.
				NGH B is getting a gauge packet
				NGH B is getting a gauge packet with the expected first 3 bytes in the header
				Discovered KMega  was sending the current position (always 0), not the desired position...
				It's alive!!! (Tested NGH B w/ monoprop and charge only)
			
			
		Troubleshooting ideas:
			Understand what data NGH B is getting.
			Redirect GaugePacketB to KKIM instead of NGH B and inspect the packet contents.
			
			Re-inspect added code...
		
			
	Tweak NUMBER_OF_NEEDLE_POSITIONS
	Tweak packet send rate to reduce jitter

Tie button to gauge calibration

Enable Intake Air...

Change Camera Mode when switching between RKT/PLN/RVR

Phone display...
	Essentials:
		APO: (Apoapsis)
		ALT: (Altitude)
		PER: (Periapsis)
		ttA/P (time to Apoapsis/Periapsis)
		Speed (m/s) + (speed type)
		Current draw (mA)
		

Implement TRIM

Fix action groups: duplicate signals sometimes sent? PV and LADR seem ok, but AG1+etc "flaky".

KMega: Integrate MUTE switch. If ON:
	a) All possible outputs are disabled
	b) OutputRefreshPackets are ignored

Maybe:
Integrate ATNV (autonavigation) momentary switch
Rename master > main
Find a way to stop the kmega COM port from changing periodically
Consider using PlatformIO (+VS Code?) instead of Arduino IDE

See KCP2_Lessons_Learned.txt