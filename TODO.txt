SOFTWARE ==================================================================================================================================

Add functionality to KKIM to be able to support switch from Flight Mode to the Space Center and ...

Understand how KKIM can be launched via JAR

Solve stepper motor performance
	Re-enable commented sections (search keyword "TODO: Stepper Logic disabled until performance is fixed (do not modify)"
	Add interrupt logic for stepper motors (don't let the packet processing slow down the motor movement)
	Convert all other stepper motors from StepperMotor to StepperMotor2.
	Send Stepper info from KKIM to KMega

Extract all needed information from kRPC. Measure performance.
	kRPC: streams?
	Replace LED brightness (0-4095) with 1 byte (0-255).
Determine necessary loop interval time to keep things "snappy"

Change Camera Mode when switching between RKT/PLN/RVR

Phone display...

Implement TRIM

Fix action groups: duplicate signals sometimes sent? PV and LADR seem ok, but AG1+etc "flaky".

Maybe:
Integrate Mute switch
Integrate ATNV (autonavigation) momentary switch
Rename master > main
Find a way to stop the kmega COM port from changing periodically
Consider using PlatformIO (+VS Code?) instead of Arduino IDE

See KCP2_Lessons_Learned.txt

HARDWARE ==================================================================================================================================

Stop SP3T switch guards from turning
Physically re-do label for air density gauge (red = 100, green = 0)
Replace LEDs that are too bright or dim them with bigger resistors
Touch up weak paint areas near backlit icons
Paint panel behind modules to keep the gaps from looking silvery